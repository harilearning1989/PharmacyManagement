import { UsernameInputDirective } from './username-input.directive';

describe('UsernameInputDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameInputDirective();
    expect(directive).toBeTruthy();
  });
});
