server:
  port: 8085

logging:
  level:
    org.springframework: ERROR

spring:
  datasource:
    url: jdbc:oracle:thin:@//127.0.0.1:31521/XEPDB1
    username: pharmacy_mgmt
    password: dudkrish1A
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
    show-sql: true

app:
  inventory:
    allocationStrategy: EXPIRY_THEN_FIFO
    lowStockThreshold: 20
    nearExpiryDays: 60
