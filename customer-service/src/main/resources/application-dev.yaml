server:
  port: 8081

logging:
  level:
    org.springframework: ERROR

spring:
  datasource:
    url: jdbc:oracle:thin:@//127.0.0.1:31521/XEPDB1
    username: pharmacy_mgmt
    password: dudkrish1A
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
    show-sql: true